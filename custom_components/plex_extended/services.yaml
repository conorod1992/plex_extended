search:
  name: Search Plex
  description: Search media titles in the configured Plex libraries and return matching items.
  fields:
    config_entry_id:
      name: Plex server
      description: Plex Extended server to query. Optional when only one server is configured.
      selector:
        config_entry:
          integration: plex_extended
    query:
      name: Query
      description: Title or search text.
      required: true
      selector:
        text:
    search_types:
      name: Media types
      description: Plex media types to include in the search.
      default:
        - movie
        - show
      selector:
        select:
          multiple: true
          options:
            - movie
            - show
            - season
            - episode
            - artist
            - album
            - track
            - collection
    limit:
      name: Limit
      default: 10
      selector:
        number:
          min: 1
          max: 50
          mode: box
    library:
      name: Library
      description: Optional exact Plex library name. Use library ID if names are duplicated.
      selector:
        text:
    library_id:
      name: Library ID
      description: Optional stable Plex library section ID returned by List libraries.
      selector:
        text:
    include_summary:
      name: Include summary
      default: true
      selector:
        boolean:
    include_technical:
      name: Include technical metadata
      description: Fetch codec, resolution, bitrate and related media metadata. This can require extra Plex requests.
      default: false
      selector:
        boolean:

query_library:
  name: Query Plex library
  description: Find Plex media using structured filters such as genre, people, year, watch state, runtime, quality, ratings and dates.
  fields:
    config_entry_id:
      name: Plex server
      description: Plex Extended server to query. Optional when only one server is configured.
      selector:
        config_entry:
          integration: plex_extended
    media_type:
      name: Media type
      description: Type of Plex item to return.
      required: true
      selector:
        select:
          options:
            - movie
            - show
            - season
            - episode
            - artist
            - album
            - track
    library:
      name: Library
      description: Optional Plex library name. If omitted, Plex Extended uses it automatically only when exactly one compatible library exists.
      selector:
        text:
    library_id:
      name: Library ID
      description: Optional stable Plex library section ID returned by List libraries.
      selector:
        text:
    title:
      name: Title contains
      description: Optional partial title filter.
      selector:
        text:
    genres:
      name: Any genre
      description: Match any supplied genre. A single value can be entered in the UI; YAML can supply a list.
      selector:
        text:
    genres_all:
      name: All genres
      description: Require all supplied genres. A single value can be entered in the UI; YAML can supply a list.
      selector:
        text:
    actors:
      name: Actors
      description: Match any supplied actor. YAML can supply a list.
      selector:
        text:
    directors:
      name: Directors
      description: Match any supplied director. YAML can supply a list.
      selector:
        text:
    collections:
      name: Collections
      description: Match any supplied Plex collection. YAML can supply a list.
      selector:
        text:
    content_ratings:
      name: Content ratings
      description: Match any supplied content rating, such as PG-13 or TV-MA. YAML can supply a list.
      selector:
        text:
    studios:
      name: Studios
      description: Match any supplied studio. YAML can supply a list.
      selector:
        text:
    year:
      name: Exact year
      selector:
        number:
          min: 0
          max: 9999
          mode: box
    year_min:
      name: Earliest year
      description: Inclusive minimum year.
      selector:
        number:
          min: 0
          max: 9999
          mode: box
    year_max:
      name: Latest year
      description: Inclusive maximum year.
      selector:
        number:
          min: 0
          max: 9999
          mode: box
    decade:
      name: Decade
      description: Decade beginning year, for example 1980.
      selector:
        number:
          min: 0
          max: 9990
          step: 10
          mode: box
    watched_state:
      name: Watched state
      default: any
      description: Plex-native watched state filter. For TV libraries, Plex may evaluate unwatched state at episode level.
      selector:
        select:
          options:
            - any
            - watched
            - unwatched
            - in_progress
    resolutions:
      name: Resolutions
      description: Match Plex resolution values such as 4k, 1080 or 720. YAML can supply a list.
      selector:
        text:
    hdr:
      name: HDR state
      default: any
      selector:
        select:
          options:
            - any
            - hdr
            - sdr
    critic_rating_min:
      name: Minimum critic rating
      selector:
        number:
          min: 0
          max: 10
          step: 0.1
          mode: box
    critic_rating_max:
      name: Maximum critic rating
      selector:
        number:
          min: 0
          max: 10
          step: 0.1
          mode: box
    audience_rating_min:
      name: Minimum audience rating
      selector:
        number:
          min: 0
          max: 10
          step: 0.1
          mode: box
    audience_rating_max:
      name: Maximum audience rating
      selector:
        number:
          min: 0
          max: 10
          step: 0.1
          mode: box
    user_rating_min:
      name: Minimum user rating
      selector:
        number:
          min: 0
          max: 10
          step: 0.1
          mode: box
    user_rating_max:
      name: Maximum user rating
      selector:
        number:
          min: 0
          max: 10
          step: 0.1
          mode: box
    duration_min_minutes:
      name: Minimum runtime
      description: Inclusive minimum runtime in minutes.
      selector:
        number:
          min: 0
          mode: box
    duration_max_minutes:
      name: Maximum runtime
      description: Inclusive maximum runtime in minutes.
      selector:
        number:
          min: 0
          mode: box
    added_after:
      name: Added after
      description: Plex date such as 2026-01-01, or a relative value such as 30d.
      selector:
        text:
    added_before:
      name: Added before
      description: Plex date such as 2026-01-01, or a relative value such as 30d.
      selector:
        text:
    last_viewed_after:
      name: Last viewed after
      description: Plex date such as 2026-01-01, or a relative value such as 30d.
      selector:
        text:
    last_viewed_before:
      name: Last viewed before
      description: Plex date such as 2026-01-01, or a relative value such as 30d.
      selector:
        text:
    sort_by:
      name: Sort by
      selector:
        select:
          options:
            - title
            - year
            - added_at
            - last_viewed_at
            - critic_rating
            - audience_rating
            - user_rating
            - duration
            - resolution
    sort_order:
      name: Sort order
      description: If omitted, title sorts ascending and other fields sort descending.
      selector:
        select:
          options:
            - asc
            - desc
    limit:
      name: Limit
      default: 10
      selector:
        number:
          min: 1
          max: 50
          mode: box
    include_summary:
      name: Include summary
      default: true
      selector:
        boolean:
    include_technical:
      name: Include technical metadata
      description: Fetch codec, resolution, bitrate and related metadata for each result. This can require extra Plex requests.
      default: false
      selector:
        boolean:

recently_added:
  name: Recently added
  description: Return recently added Plex media.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended
    limit:
      name: Limit
      default: 10
      selector:
        number:
          min: 1
          max: 50
          mode: box
    library:
      name: Library
      description: Optional exact Plex library name. Use library ID if names are duplicated.
      selector:
        text:
    library_id:
      name: Library ID
      description: Optional stable Plex library section ID returned by List libraries.
      selector:
        text:
    media_types:
      name: Media types
      selector:
        select:
          multiple: true
          options:
            - movie
            - show
            - season
            - episode
            - artist
            - album
            - track
            - collection
    include_summary:
      name: Include summary
      default: true
      selector:
        boolean:

recently_watched:
  name: Recently watched
  description: Return Plex play history, optionally filtered by Plex user, library or media type.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended
    limit:
      name: Limit
      default: 10
      selector:
        number:
          min: 1
          max: 50
          mode: box
    library:
      name: Library
      description: Optional exact Plex library name. Use library ID if names are duplicated.
      selector:
        text:
    library_id:
      name: Library ID
      description: Optional stable Plex library section ID returned by List libraries.
      selector:
        text:
    user:
      name: Plex user
      description: Optional Plex user name. If omitted, history for all users visible to the server token is returned.
      selector:
        text:
    user_id:
      name: Plex user ID
      description: Optional stable Plex account ID returned by List users. If both name and ID are supplied, they must refer to the same user.
      selector:
        text:
    media_types:
      name: Media types
      selector:
        select:
          multiple: true
          options:
            - movie
            - show
            - season
            - episode
            - artist
            - album
            - track
            - collection
    include_summary:
      name: Include summary
      default: true
      selector:
        boolean:

continue_watching:
  name: Continue watching
  description: Return the Plex Continue Watching hub.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended
    limit:
      name: Limit
      default: 10
      selector:
        number:
          min: 1
          max: 50
          mode: box
    library:
      name: Library
      description: Optional exact Plex library name. Use library ID if names are duplicated.
      selector:
        text:
    library_id:
      name: Library ID
      description: Optional stable Plex library section ID returned by List libraries.
      selector:
        text:
    include_summary:
      name: Include summary
      default: true
      selector:
        boolean:

on_deck:
  name: On Deck
  description: Return Plex On Deck items.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended
    limit:
      name: Limit
      default: 10
      selector:
        number:
          min: 1
          max: 50
          mode: box
    library:
      name: Library
      description: Optional exact Plex library name. Use library ID if names are duplicated.
      selector:
        text:
    library_id:
      name: Library ID
      description: Optional stable Plex library section ID returned by List libraries.
      selector:
        text:
    include_summary:
      name: Include summary
      default: true
      selector:
        boolean:

media_details:
  name: Media details
  description: Return detailed metadata for a Plex item by rating key.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended
    rating_key:
      name: Rating key
      description: Plex rating key returned by another Plex Extended query.
      required: true
      selector:
        text:
    include_technical:
      name: Include technical metadata
      default: true
      selector:
        boolean:

list_libraries:
  name: List libraries
  description: Return Plex library names, stable IDs and types.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended

list_users:
  name: List users
  description: Return Plex users and stable account IDs for watch-history filtering.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended

test_connection:
  name: Test connection
  description: Test the configured Plex server connection and return server details.
  fields:
    config_entry_id:
      name: Plex server
      selector:
        config_entry:
          integration: plex_extended
